#include "common.h"

const Vector2 WIN_RES = {1600, 900};
const Color BACKGROUND_COLOR = {
    .r = (uint8_t) (255 * 0.10f),
    .g = (uint8_t) (255 * 0.16f),
    .b = (uint8_t) (255 * 0.25f),
    .a = (uint8_t) (255 * 1.00f),
};
